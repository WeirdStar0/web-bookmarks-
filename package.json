{
  "name": "web-bookmarks",
  "version": "1.1.0",
  "description": "现代化书签管理系统 - 基于 Cloudflare Workers 和 D1",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 execute bookmarks-db --local --file=./schema.sql && wrangler d1 execute bookmarks-db --local --file=./migrations/002_add_indexes.sql",
    "migrate:remote": "wrangler d1 execute bookmarks-db --remote --file=./schema.sql && wrangler d1 execute bookmarks-db --remote --file=./migrations/002_add_indexes.sql",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["bookmarks", "cloudflare", "workers", "d1", "hono"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hono": "^4.10.6"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.11.1",
    "@cloudflare/workers-types": "^4.20251121.0",
    "vitest": "^4.0.16",
    "wrangler": "^4.50.0"
  }
}
